# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.expo/
.turbo/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Native
ios/Pods/
ios/build/
android/.gradle/
android/build/
android/app/build/
*.keystore
!debug.keystore

# Expo
.expo/
.expo-shared/
expo-env.d.ts

# Mobile build artifacts
*.ipa
*.apk
*.aab

# Temporary holding for admin API routes (will move to apps/admin in next stage)
_admin-api-tmp/

# Temporary reference files copied from old Visdom for migration (do not commit — may contain secrets like old admin password)
_old-*
*.tmp

# Stage 3.3 video manifest tmp file (lives only on R2)
video-manifest.json

# Apple authentication keys (ASC API, APNs, Sign In with Apple) — never commit
*.p8
AuthKey_*.p8

*.zip

# UI design reference images (local only, 48MB)
design-refs/
__pycache__/

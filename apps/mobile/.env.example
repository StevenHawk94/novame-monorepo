# @novame/mobile environment variables
#
# Copy this file to .env.local and fill in the actual values.
#   cp apps/mobile/.env.example apps/mobile/.env.local
#
# .env.local is gitignored. Never commit it.
#
# All EXPO_PUBLIC_* values are baked into the app bundle and visible
# to anyone who downloads the app — only put values here that are
# safe to be public (anon keys, public API URLs).
#
# Server-only secrets (SUPABASE_SERVICE_ROLE_KEY, AIRWALLEX_API_KEY,
# RESEND_API_KEY, etc.) belong in apps/api environment, NOT here.

# Supabase project URL — same as admin/api NEXT_PUBLIC_SUPABASE_URL
EXPO_PUBLIC_SUPABASE_URL=

# Supabase anon (publishable) key — same as admin/api NEXT_PUBLIC_SUPABASE_ANON_KEY
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# NovaMe API base URL
#   Local development: http://localhost:3001
#   Production:        https://novame-monorepo-api.vercel.app
EXPO_PUBLIC_API_URL=

# Google OAuth Client IDs (stage 3.4 step 7)
# Web Client ID: used by GoogleSignin SDK to request idToken with Web audience (Supabase verifies)
# iOS Client ID: used by GoogleSignin SDK on iOS for native Sign-In sheet
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Crash reporting (Sentry). Set the same value in EAS production env.
EXPO_PUBLIC_SENTRY_DSN=

# Development-only override for testing the shared Meta/TikTok privacy gate.
# Accepted values: eea_uk | other
EXPO_PUBLIC_ADS_PRIVACY_REGION_OVERRIDE=

# TikTok App Events SDK native build credentials.
# These are deliberately NOT EXPO_PUBLIC: the config plugin injects only the
# selected platform's values into its native app during prebuild. As with all
# mobile SDK credentials, the resulting values are present in the signed app
# binary; never reuse them as server-side authorization credentials.
TIKTOK_IOS_APP_ID=
TIKTOK_IOS_TIKTOK_APP_ID=
TIKTOK_IOS_ACCESS_TOKEN=

/**
 * @novame/core/constants/recording
 *
 * Recording length limits.
 */

export const DAILY_RECORDING_LIMIT_SECONDS = 600

export const MIN_RECORDING_SECONDS = 20

export const MAX_SECONDS_PER_RECORD = 600

/** Minimum character count for a typed-mode wisdom (publish disabled below this). */
export const MIN_TYPED_CHARS = 100
